# Codex Quota HUD v1.3.0

> Local installed-candidate record. Packaging and the nine-scenario isolated
> installer matrix passed. The real Setup install, installed binary identity,
> data preservation, startup/shortcut/uninstall state, and binary launch gates
> passed; this narrower result does not cover three unrun installed-smoke gates.
> Bounded installed Designer GUI smoke is `6 PASS / 3 PARTIAL / 1 NOT RUN`;
> user practical acceptance remains open. This is not yet a public release.
> Public `v1.2.3` remains unchanged.

Candidate product source is
`aecaea11f3ecf60738f46d8907ade63b3e081cb8`. Fresh Task 8 source evidence is
Core `75/75`, Skins `375/375`, App/UI `625/625`, and Designer `424/424`, for
`1499/1499` with zero failed/skipped. Release build warnings/errors were `0/0`
and `git diff --check` passed.

Candidate asset evidence:

- Setup: `100,056,640` bytes / SHA-256
  `ceccf8c060f52600ef9227a7257d58817aaaf742b0a895f07d2efe613661e3ff`
- normal-HUD ZIP: `68,341,872` bytes / SHA-256
  `9b91351dd017ea0b868f752f6901d5923a8988361f22fff801ef3d4dc4490fba`
- `SHA256SUMS.txt`: `196` bytes / SHA-256
  `9f0be51c502678cfc8a1f2ca394697888a79266a82f3aa92d5b74ac6d43df771`
- Manifest: exactly two lowercase lines; both declared hashes match.
- ZIP: exactly App, README, LICENSE, install script, and uninstall script; no
  Designer. Publish: exactly App plus `designer\CodexQuotaHud.SkinDesigner.exe`.
- App and Designer: file version `1.3.0.0`, product version
  `1.3.0+aecaea11f3ecf60738f46d8907ade63b3e081cb8`; Setup product version `1.3.0`.
- Setup, App, and Designer: `NotSigned`, with no signer or timestamper.
- nine-scenario isolated installer matrix: PASS (`9/9`); all seven normal and
  both committed-cleanup failure scenarios passed with checked cleanup; final
  smoke-root count `0`.
- real Setup/install identity, data-preservation, startup/shortcut/uninstall,
  and binary-launch gates: PASS. Setup exited `0`; installed App/Designer match
  publish and report `1.3.0.0` plus full `aecaea1`; uninstall is `1.3.0`; 34
  product-state files and two exchange packages were preserved, with only
  `LastSuccessfulRefresh` advancing normally.
- ten installed Designer GUI rows: `6 PASS / 3 PARTIAL / 1 NOT RUN`
- installed tray-menu operation: `NOT RUN`
- installed old-package import: `NOT RUN`
- formal HUD guide/audition isolation after Designer closes: `NOT RUN`
- user practical acceptance: `NOT RUN`

The real maintainer upgrade selected startup plus Designer and intentionally
omitted the normal desktop icon to preserve this machine's preference. Before
the separate local restoration, Setup left zero product desktop shortcuts,
proving that it removed the preview entry and did not create a normal one. The
maintainer-only preview shortcut was then restored from its exact backup with
the standard App target and exact `--preview`; this is not Setup behavior.
Formal HUD and installed Designer are running, and Designer is left open for
the user acceptance handoff. The bounded installed-GUI fallback directly
passed text offset, line gap, guides/click-through, refresh-state restoration,
display-mode semantics, and export-picker rows. Undo/redo/save/reopen remains
partial because installed UI exposes no Undo/Redo entry and the later identity
gate blocked the save/reopen subset; `0/0` lacks a same-context v1.2.3 visual;
the blank animation fixture could not make every selected channel distinct;
Apply was not run after its exact PID/HWND/title gate failed before input.
The installed tray menu was not operated, no legacy package was imported
through the installed product, and the formal HUD was not directly inspected
for guide/audition leakage after the Designer closed. Source automated
compatibility/isolation tests do not promote those installed gates.
Overall Task 9 remains `PARTIAL`.

## 简体中文

`v1.3.0` 补齐皮肤作者的文字构图与预览验收工具：

- 新增“文字整体上下偏移”（`-32..32 DIP`）和“数字/时间间距”
  （`-16..32 DIP`）两个持久化参数。旧 v1.2.x 包缺字段时按 `0/0` 读取，
  原视觉不变；新输出仍使用 schema `1`，但最低 HUD 自动声明为 `1.3.0`。
- 新增默认关闭、不可点击拦截且只存在于设计器预览中的构图参考线。
- 新增不会写入草稿、撤销历史、应用或导出数据的单项动画预演。
- 明确显示 Dual 外圈为 5 小时、内圈为每周，并同步说明 5h、Week、None。
- Windows 原生导出选择器会从 `Documents\Codex Quota HUD Skins` 开始，
  文件名栏只放叶子 `.cqskin` 名称。
- 应用与导出结果显示准确的皮肤名、版本、ID、最终目录和真实激活状态；
  “已安装但未确认切换”不会写成实时切换成功。

Setup 的普通用户契约不变：开机启动与正式版桌面快捷方式默认勾选且允许取消，
快捷方式不带 `--preview`；皮肤设计器可见但默认不勾选；ZIP 只含正式 HUD
后备包；Setup 会移除开发预览入口。维护者机器上的开发预览快捷方式仅作为本地
状态，在产品安装验证后另行恢复为准确的 `--preview`，不是 Setup 行为。

候选资产名称：

- `CodexQuotaHud-Setup-v1.3.0.exe`
- `CodexQuotaHud-v1.3.0-win-x64.zip`
- `SHA256SUMS.txt`

实际 Authenticode 检查显示 Setup、App、Designer 均为 `NotSigned`，且没有签名者
或时间戳。十行已安装 GUI 冒烟为 `6 PASS / 3 PARTIAL / 1 NOT RUN`；installed 托盘
菜单操作、旧包导入、Designer 关闭后正式 HUD 不泄露参考线/单项预演三项均为
`NOT RUN`。源码自动化兼容/隔离测试不能替代这些 installed smoke。仍须处理或明确
接受未闭环项，并完成用户实操确认后，才允许进入 `main`、标签或 GitHub Release。

## English

`v1.3.0` completes the Skin Designer's text-composition and preview acceptance
tooling:

- Persisted Text vertical offset (`-32..32 DIP`) and Number/time line-gap
  increment (`-16..32 DIP`) controls. Missing v1.2.x properties default to
  `0/0`; new output remains schema `1` and requires HUD `1.3.0`.
- Default-off, click-through composition guides confined to Designer preview.
- Non-persistent per-channel animation audition that never enters draft,
  history, Apply, or Export data.
- Explicit Dual outer = 5h / inner = Week guidance, plus 5h, Week, and None
  explanations.
- A native Windows export picker rooted in
  `Documents\Codex Quota HUD Skins` with only a leaf filename.
- Exact Apply/Export feedback for skin name, version, ID, final directory, and
  truthful activation disposition.

The ordinary-user Setup contract is unchanged: startup and the normal desktop
shortcut default selected and may be deselected; the shortcut has no
`--preview`; Designer is visible and unchecked by default; ZIP is normal-HUD-
only; Setup removes Developer Preview entries. A maintainer-only preview
shortcut is separate local state restored with exact `--preview` after product
installation is verified, not Setup behavior.

Authenticode measured Setup, App, and Designer as `NotSigned`, with no signer
or timestamper. Installed GUI smoke is `6 PASS / 3 PARTIAL / 1 NOT RUN`;
installed tray-menu operation, old-package import, and post-Designer-close HUD
guide/audition isolation are each `NOT RUN`. Source automated compatibility or
isolation coverage is not installed smoke. These open gates and explicit user
acceptance still block release. No `main` integration, tag, GitHub Release,
upload, or public readback is permitted. Remote release Step 7 remains
prohibited.
